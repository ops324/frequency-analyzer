# Frequency Analyzer — 仕様書

リアルタイム音響解析ツール「Frequency Analyzer（Observatory）」の仕様書。
マイク入力から周波数・音名・チューニング（セント偏差）を解析し、シネマティックな3Dビジュアライザーで可視化する単一HTMLアプリ。

- **最終更新:** 2026-07-03（UI/UX最高品質化 全7フェーズ完了 — イントロ仕上げ・OGP画像刷新・`aria-live` 追加）
- **形式:** 単一HTMLファイル（ビルド不要）
- **公開URL:** https://frequency-analyzer.vercel.app （Vercel・GitHub連携で自動デプロイ）

---

## 1. 概要

| 項目 | 内容 |
|------|------|
| 目的 | マイク音声をリアルタイム解析し、周波数・音量・音名・チューニングを表示・可視化する |
| 形態 | 単一HTML（`index.html`）。サーバー不要、静的ホスティングで動作 |
| デザイン方針 | シネマティック天文台（Cinematic Observatory）— 漆黒の宇宙的背景、ブルーム発光、フィルムグレイン、暖色×寒色のデュオトーン |
| 対象ブラウザ | Chrome / Edge / Safari / Firefox の最新版（WebGL2 + Web Audio API対応） |
| 動作要件 | マイク使用には**セキュアコンテキスト**（`https://` または `localhost`）が必須 |

---

## 2. 機能仕様

### 2.1 計測・表示
- **Frequency（周波数）**: 検出基本周波数を `Hz`（小数1桁）で表示。値は補間（lerp）で滑らかに更新。整数部と小数部を別スパンで描画し、小数部は小さめ・淡色（計器風の階層表現）。
- **Volume（音量）**: RMSから算出した `dB` を整数表示。
- **Note（音名）**: 検出周波数に最も近い音名＋オクターブを表示。オクターブは上付きの小さめ表示（例: `A⁴`）、変化記号は `♯`/`♭` グリフで描画。
- **Tuner（チューナー）**: 最近接音からの**セント偏差（±50）**を算出。
  - SVG円弧ゲージ＋イージング付き針で偏差を可視化。目盛りは5セント刻み（25セントごとにメジャー＋ −50/−25/0/+25/+50 の数字ラベル）。
  - 背景弧は cool→amber の微かなグラデーション。±5セントの「イン・チューン窓」を淡ミントの固定弧として常時表示（目標が常に見える）。
  - 針はカウンターウェイト付き＋ハブはリング＋ドット。より遅い補間で追従する半透明の**ゴースト針**がアナログ的慣性を表現（`prefers-reduced-motion` では本針に即時追従）。
  - セント偏差の数値には方向グリフを前置: シャープ側 `♯`（amber）/ フラット側 `♭`（cool）/ イン・チューン時は mint。
  - `|cents| < 5` で「In Tune」判定 → 針・円弧・ハブ・音名がミントグリーンに発光（リワード演出）。
- **Spectrum（スペクトラム）**: 対数周波数軸（40Hz–18kHz）の2Dバー表示。寒色→暖色グラデーション。

### 2.2 ビジュアライザー（Three.js）
- 二重リングの放射状リアクティブバー（周波数ビンに連動、暖色↔寒色マッピング）。
  - **InstancedMesh** による描画（136ドローコール→2）。バーの色は `instanceColor` で毎フレーム更新し、`onBeforeCompile` のシェーダーパッチで発光（emissive）にも同じ色を適用（バーごとの発光強度を実現）。
  - **非対称エンベロープ**: 立ち上がりは速く（attack 0.55）、減衰はゆっくり（release 0.06）— 音に「跳ね、落ちる」物理感。
- **環境マップ（IBL）**: `RoomEnvironment` + `PMREMGenerator` によるイメージベース環境反射。金属バーとコアに実在感のあるスペキュラ。強度はテーマ別（ダーク0.35 / ライト0.55）。ライトのバーは専用の濃色パレット（バーントアンバー `#b06a12` ↔ ディープティール `#2a7aa1`）で陶器/アルマイト調。
- **オンセットパーティクル**: スペクトラルフラックス（周波数ビンの正方向変化量）で音の立ち上がりを検出し、コアから火の粉（240粒プール、加算ブレンド）が立ち上る。適応閾値（移動平均比）+130msクールダウン。`prefers-reduced-motion` では無効。
- 中央コア（正二十面体＋ワイヤーフレーム）が音量（RMS）に連動して脈動。
- 波形リング（LineLoop）が時間波形を描画。
- **スターフィールド2層**（遠900粒・近500粒）: 近層は大きめ・速い回転・微かな明滅で奥行きを強調。
- **極座標フロア**: 同心円＋放射スポークの canvas テクスチャ（白描画→マテリアル色でテーマ着色）を円形メッシュに貼り、外周はアルファ減衰でフォグに溶ける。テーマ切替はテクスチャ色替えのみ（旧 GridHelper 再生成を廃止）。
- **UnrealBloomPass** による実ブルーム発光。**閾値（threshold）を高め**に設定し、発光を明部（明るいバー・コア）だけに限定 → 暗部は締まったまま白飛びしない。強度は音量で微増＋**オンセット時に短いキック**（+0.12〜0.2、指数減衰）でリズミカルに呼吸。
- シネマティックなカメラドリー＋マウスパララックス。
- 無音時も「呼吸する」アイドルアニメーションを継続。

### 2.3 UI / インタラクション
- **イントロオーバーレイ**: 初回は「Press to listen」。クリックでマイク許可を要求（ユーザージェスチャ要件を満たす）。段階的フェードイン演出。
  - **許可待ち状態**: `getUserMedia` の応答待ちの間、ボタンが「Waiting…」＋アイコン明滅（ステータスは「Requesting…」）。許可/拒否どちらでも状態を復元。**拒否後はボタンが「Try again」**になりリトライを促す。
  - 開始ボタンのリングパルスは3Dコアのアイドル呼吸（`sin(t*1.6)` ≈ 3.93秒周期）と同期。
  - プライバシー文言「Audio is analysed on-device — nothing leaves your browser」を常設表示（信頼性の担保）。
  - 退場時はフェード＋8pxブラーで空間に溶けるように消える。
- **開始シネマティック**（~1.6秒）: マイク許可成功時、カメラが後方（z+16, y+3）からイージング付きでドリーイン。ブルーム強度が 0.5→1.1→0.55 とスウェルし、パララックスは遷移中ブレンドイン。停止時は約0.6秒の露出ディップで静かに幕を引く。
- **マイクロインタラクション**:
  - 音名変化時に200msのティック（scale 1.045→1＋輝度フラッシュ）。
  - **イン・チューン突入時**（継続中は再発火しない）: ゲージハブから広がるSVGリップル＋3Dシーンにミントのリングパルス（プール再利用のトーラス）＋モバイルで軽い振動（`navigator.vibrate(12)`）。
  - 上記演出はすべて `prefers-reduced-motion` で無効。
- **トランスポート**: コンソール内の Listen / Stop トグル＋ステータスドット。
- **テーマ切替**: ダーク（Night / 既定）↔ ライト（Day / 暖色のスタジオ調）。`applyTheme()` が3Dシーン全体を一括最適化 — クリアカラー・フォグ・トーンマッピング露出・ブルーム閾値/強度・各ライト強度・バーの色と発光量・コア/波形/星/グリッドの色を**モード別に切替**。
  - ライトでは白飛びを避けるため、ブルームをほぼ無効化＋バーを暗め・高彩度・低発光にして明背景でも視認可能にする「昼スタジオ」処理。
- **HUD**: 四隅にブランド・サンプルレート・FFTサイズ・ステータスをモノスペースで表示。

### 2.4 SNS共有（OGP / Twitter Card）
- URL を LINE・X(Twitter)・Discord・Slack 等に貼ると、専用のプレビュー画像（`og.png`）が表示される。
- `index.html` の `<head>` に Open Graph（`og:title` / `og:description` / `og:url` / `og:image` ＋ `1200×630`）と Twitter Card（`summary_large_image`）のメタタグを設定。`og:image` は絶対URL。
- 画像はアプリの "Observatory" な世界観（ダーク背景・ブルーム・Amber×Cool×Mint・EB Garamond / Geist Mono）を表すブランド画像。測定結果ではなくサンプル表示（`A⁴ · 440.0 Hz · in tune`）＋**精密ゲージ**（5セント刻みティック・数字目盛り・イン・チューン窓・カウンターウェイト針、`index.html` の `buildTicks()` をミラーした小スクリプトで生成）＋スペクトラム装飾。
- 画像ソースは `assets/og-source.html`（CSSのみで1200×630を再現、Three.js/Web Audio は不使用）。Chrome ヘッドレスでスクリーンショットして `og.png` を再生成する:
  ```bash
  "/Applications/Google Chrome.app/Contents/MacOS/Google Chrome" \
    --headless=new --hide-scrollbars --force-device-scale-factor=1 \
    --window-size=1200,630 --virtual-time-budget=3000 \
    --screenshot="og.png" "file://$PWD/assets/og-source.html"
  ```
- ※ SNS側はOGPをキャッシュするため、更新後すぐに反映されない場合がある。

### 2.5 状態とエラー処理
| 状態 | 挙動 |
|------|------|
| Ready | イントロ表示、ビジュアライザーはアイドル動作 |
| Listening | マイク解析中。各値・ゲージ・スペクトラム・3Dが反応 |
| Stopped | 解析停止、表示は「—」にリセット、スペクトラムクリア |
| マイク拒否 | イントロ内に「Microphone access denied …」を表示（クラッシュしない） |

---

## 3. 技術仕様

### 3.1 技術スタック
- HTML / CSS / Vanilla JS（ESモジュール、ビルド不要）
- **Three.js 0.160.0**（`<script type="importmap">` + ESM、CDN: jsDelivr）
  - 後処理: `EffectComposer` / `RenderPass` / `UnrealBloomPass` / `OutputPass`
- **Web Audio API**（`AnalyserNode`, `getUserMedia`）
- フォント: `EB Garamond`（ディスプレイ）/ `Geist Mono`（数値・データ、tabular figures）— Google Fonts

### 3.2 ピッチ検出アルゴリズム
- **NSDF（McLeod Pitch Method）** ベースの自己相関。
  - `fftSize = 4096`、`smoothingTimeConstant = 0.8`。
  - 探索レンジ: 約 60Hz–1400Hz（`maxLag = sr/60`, `minLag = sr/1400`）。
  - RMS閾値 `0.003` 未満は無音として無視。
  - 放物線補間でピーク位置を精緻化。
- セント計算: `cents = (12·log2(f/440)+69 − round(...)) × 100`、±50にクランプ。

### 3.3 パフォーマンス / アクセシビリティ
- **性能ティア**: `(max-width:760px), (pointer:coarse)` でモバイル判定。モバイルは pixelRatio 上限 **1.5**（デスクトップ2）、バー 60+40（デスクトップ 84+52）、星 450+250（900+500）、ブルームを半解像度で実行。起動時に選択ティアを `console.info` で出力。
- **適応劣化**: フレーム時間の移動平均が 24ms 超を3秒継続すると pixelRatio を 0.25 刻みで自動降下（下限 1.0）。
- **ポートレート対応カメラ**: 縦画面では狭い水平FOVに合わせてカメラを後退（z 30→44）し、注視点を下げてリング群をコンソール上の空きスペースに配置。
- タッチ環境ではポインタパララックスを無効化。ボタンは `touch-action: manipulation`。
- **セーフエリア**: コンソール・HUD・テーマボタンは `env(safe-area-inset-*)` を尊重（ノッチ/ホームバー回避）。
- モバイルの Listen ボタンは **min-height 52px**（≤760px では48px）の親指ターゲット。≤480px はゲージをヒーローにした1カラム再構成（ゲージ → 横一列の Frequency/Volume ストリップ → スペクトラム36px → 全幅トランスポート）。
- `composer` / キャンバスはリサイズ時に再構成。
- 停止時に `MediaStream` / `AudioContext` を解放。
- `prefers-reduced-motion` でグレイン・パララックス・各種アニメーションを無効化。
- セマンティックなボタン、`aria-label`、`:focus-visible` リング対応。ステータステキストは `aria-live="polite"`（状態変化をスクリーンリーダーに通知）。
- **コントラスト（WCAG）**: 両テーマで AA 準拠を計算で再検証（2026-07-03）。主要数値・音名 ≈17:1、cents（text-dim）6.7:1、微小ラベルはダーク5.1:1・ライト5.4:1、amber数値はライトパネル上4.6:1、mint（In Tune、ライト `#0d764a`）5.4:1。**Listenボタンはライトで白文字**（amber地に黒は4.1:1でAA未達 → 白4.8:1に変更、ダークは黒12.0:1のまま）。半透明パネルは不透明度・縁・影を強化し3D背景から確実に分離。

---

## 4. デザインシステム

### 4.1 カラーパレット
| トークン | ダーク | ライト | 用途 |
|---------|--------|--------|------|
| 背景 (--bg) | `#04060b` | `#e9e2d2` | ページ／ステージ |
| 3Dクリア (--clear) | `#04060b` | `#cfc4aa` | WebGLクリアカラー（ライトは沈めてバーを視認可能に） |
| テキスト | `#f1f3f8` | `#1a1712` | 主要テキスト |
| 薄文字 (--text-faint) | `#7b8499` | `#6f6654` | ラベル/単位（AA準拠まで引き上げ） |
| Amber（主アクセント） | `#f2c17d` | `#a4630f` | 周波数・主要発光 |
| Cool（寒色） | `#7cc4e2` | `#256f93` | 奥行き・寒色側マッピング |
| Mint（成功） | `#5fe3a4` | `#0d764a` | **In Tune時のみ**使用 |

### 4.2 タイポグラフィ
- ディスプレイ: `EB Garamond`（タイトル、音名）— 端正で上品なクラシックセリフ体
- データ／数値: `Geist Mono`（フォールバック `JetBrains Mono`、等幅・tabular-nums）
- マイクロラベル: モノスペース、大文字、ワイドトラッキング

### 4.3 マテリアル（ガラス）
- 半透明パネル（コンソール等）は多層構造で構成: 1px外縁（`--border`）＋グラデーション縁光（`::after` + `mask-composite: exclude` による1pxリング、上端明→側面へ消失）＋内側トップハイライト（`--glass-highlight`）＋近接/拡散の2層ドロップシャドウ＋ガラス内ノイズ（`::before` のSVGタービュランス、backdrop-filter のバンディング抑制）。
- ゾーン仕切りは「透明→hairline→透明」の縦グラデーションで機械加工的な質感に。
- ボタンは `@media (hover:hover)` 環境でのみ 1px 浮き上がるホバー状態を持つ（タッチ環境ではスティッキーホバー回避）。
- タイポグラフィサイズはトークンで一元管理: `--fs-note` / `--fs-data` / `--fs-mid` / `--fs-label` / `--fs-micro`。

### 4.4 モーション
- ページロード: 段階的リビール（`animation-delay` によるスタガー）。
- 数値: lerp による滑らかな追従（ジッター抑制）。
- グレイン: ステップアニメーション。針: イージングで目標値へ収束。

---

## 5. ファイル構成

```
周波数測定器/
├── index.html            # 本体（マークアップ・CSS・描画/音声JS）。純粋関数は src/ から import。<head> に OGP/Twitter メタ
├── og.png                # SNS共有用プレビュー画像（1200×630、ルート配信）
├── assets/
│   └── og-source.html    # og.png のソース（CSSのみで世界観を再現、Chromeヘッドレスで再生成）
├── src/
│   └── pitch.js          # ピッチ/音楽理論の純粋関数（freqToNote / centsFromNearest / detectPitch）
├── tests/
│   └── pitch.test.mjs    # node:test によるユニットテスト
├── package.json          # npm test（node --test）。ビルド依存なし
├── 仕様書.md             # 本ドキュメント
├── .github/workflows/
│   └── ci.yml            # GitHub Actions（push / PR でテスト実行）
└── .claude/              # 開発用ヘルパー（プレビューサーバー設定）
    ├── launch.json
    └── static-server.cjs
```

> **テスト**: `npm test`（依存ゼロ、Node 標準 `node:test`）。`detectPitch` は合成正弦波からの周波数復元と無音判定を検証。CI（`.github/workflows/ci.yml`）が `main` への push と PR で自動実行。

---

## 6. 起動・デプロイ

### 6.1 ローカル起動（マイク利用にはセキュアコンテキストが必須）
```bash
cd 周波数測定器
python3 -m http.server 8000
# → http://localhost:8000/ を開く
```

### 6.2 デプロイ（Vercel）
- リポジトリ `ops324/frequency-analyzer`（公開）に Vercel が連携し、`main` への push で**自動デプロイ**。
- 静的ファイル配信（ビルド設定不要）。公開URL: **https://frequency-analyzer.vercel.app**（HTTPS = セキュアコンテキストのためマイク利用可）。
- 更新手順: `git push origin main` → Vercel が Production ビルド → 公開URLに反映（通常1〜2分）。
